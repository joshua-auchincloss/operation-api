namespace foo;

use bar_corp::baz;
use bar_corp::baz::BazOrString;

// single line comment on type
type Abc = oneof str | null;

/*
	some multiline comment.
*/
struct TestComplexMessage {
	// Field comment
	a:  i32,
	/*
		Multiline field comment
	*/
	b:  i64,
	c:  str,
	d?: bool,
	e?: str,
	f:  BazOrString,
	g:  Abc,
	h:  baz::Baz,
};
