namespace test;

#![version(1)]

enum ErrorCode {
    internal_error
};

struct ErrorDesc {
    desc: str,
    code: ErrorCode,
};

error ServerError {
    known(ErrorDesc),
    unknown {
        desc: str
    }
}
